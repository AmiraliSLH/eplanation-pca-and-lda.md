کاهش بُعد داده‌ها با استفاده از PCA و LDA (نمایش دوبعدی)

در این پروژه، یک مجموعه‌داده پزشکی انتخاب شده و دو تکنیک کاهش بُعد PCA و LDA بر روی آن پیاده‌سازی شده‌اند.
هدف اصلی، کاهش ابعاد داده‌ها به دو بُعد و بررسی و مقایسه‌ی نمایش بصری حاصل از هر روش می‌باشد.

📊 دیتاست مورد استفاده

Breast Cancer Wisconsin Dataset

این دیتاست شامل ویژگی‌هایی است که از تصاویر توده‌های پستان استخراج شده‌اند و یکی از دیتاست‌های پرکاربرد در مسائل یادگیری ماشین پزشکی به شمار می‌رود.
داده‌ها دارای دو برچسب «خوش‌خیم» و «بدخیم» هستند که استفاده از روش LDA را امکان‌پذیر می‌سازد.

🧠 روش‌های مورد استفاده
🔹 PCA (تحلیل مؤلفه‌های اصلی)

بدون در نظر گرفتن برچسب کلاس‌ها

تمرکز بر حفظ بیشترین میزان واریانس داده‌ها

کاهش بُعد داده‌ها به ۲ مؤلفه اصلی

🔹 LDA (تحلیل تفکیکی خطی)

استفاده از اطلاعات برچسب‌ها

هدف، بیشینه‌سازی فاصله و جداسازی بین کلاس‌ها

کاهش بُعد داده‌ها به ۲ مؤلفه تفکیکی

🛠️ مراحل پیاده‌سازی

بارگذاری دیتاست

نرمال‌سازی ویژگی‌ها با استفاده از StandardScaler

اعمال PCA با مقدار n_components=2

اعمال LDA با مقدار n_components=2

رسم و نمایش نتایج به‌صورت نمودارهای دوبعدی

📈 نتایج حاصل

در نمودار PCA، داده‌ها بر اساس بیشترین واریانس پراکنده شده‌اند، اما هم‌پوشانی بین کلاس‌ها دیده می‌شود.

در نمودار LDA، تفکیک کلاس‌ها واضح‌تر است، زیرا از اطلاعات برچسب‌ها بهره گرفته شده است.

برای این دیتاست پزشکی، روش LDA نمایش بصری مناسب‌تری نسبت به PCA ارائه می‌دهد.

📌 جمع‌بندی

روش PCA بیشتر برای فشرده‌سازی داده‌ها و کاهش نویز کاربرد دارد،
در حالی که LDA در مسائل طبقه‌بندی و جداسازی کلاس‌ها عملکرد مؤثرتری نشان می‌دهد.

📚 ابزارها و کتابخانه‌های استفاده‌شده

Python

scikit-learn

matplotlib

numpy

👤 درس: ماشین لرنینگ
📝 تمرین: کاهش بُعد و نمایش دوبعدی داده‌ها با استفاده از PCA و LDA
