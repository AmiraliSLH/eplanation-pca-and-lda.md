. Vision Transformer (ViT)

An Image is Worth 16×16 Words: Transformers for Image Recognition at Scale

نویسندگان: Alexey Dosovitskiy و همکاران

سال / محل انتشار: 2020 – arXiv / ICLR

ایده اصلی:
این مقاله برای نخستین‌بار نشان داد که می‌توان تصویر را به مجموعه‌ای از پچ‌ها تقسیم کرد و آن‌ها را مشابه توکن‌های متنی به یک Transformer داد، بدون استفاده از شبکه‌های CNN.

نکته کلیدی:
با پیش‌آموزش در مقیاس بسیار بزرگ و فاین‌تیون مناسب، ViT روی ImageNet به دقت بسیار بالایی دست پیدا می‌کند.

دیتاست‌ها: JFT-300M (پیش‌آموزش)، ImageNet (ارزیابی)

پیاده‌سازی: JAX/Flax و PyTorch

تعداد ارجاعات: 75,000+

۲. Swin Transformer

Hierarchical Vision Transformer using Shifted Windows

نویسندگان: Ze Liu و همکاران

سال / محل انتشار: 2021 – ICCV

ایده اصلی:
استفاده از پنجره‌های توجه محلی با جابه‌جایی (Shifted Windows) برای کاهش هزینه محاسباتی attention و در عین حال حفظ ارتباط بین نواحی تصویر.

ویژگی مهم:
ساختار سلسله‌مراتبی که آن را به یک backbone عمومی برای وظایف مختلف بینایی ماشین تبدیل می‌کند.

دیتاست‌ها: ImageNet، COCO، ADE20K

پیاده‌سازی: PyTorch

تعداد ارجاعات: 24,000+

۳. Masked Autoencoders (MAE)

Masked Autoencoders Are Scalable Vision Learners

نویسندگان: Kaiming He و همکاران

سال / محل انتشار: 2022 – CVPR

ایده اصلی:
ماسک‌کردن بخش بزرگی از پچ‌های تصویر و آموزش مدل برای بازسازی آن‌ها، به‌عنوان یک روش ساده اما بسیار مؤثر در یادگیری خودنظارتی.

مزیت اصلی:
تولید نمایش‌های قدرتمند و قابل انتقال به وظایف مختلف پایین‌دستی.

دیتاست: ImageNet-1K

پیاده‌سازی: PyTorch

تعداد ارجاعات: 8,800+

۴. Segment Anything (SAM)

نویسندگان: Alexander Kirillov و همکاران

سال / محل انتشار: 2023 – ICCV

ایده اصلی:
معرفی یک مدل پایه برای تقسیم‌بندی تصویر که می‌تواند با دریافت پرامپت (نقطه، کادر یا متن) تقریباً هر شیئی را در تصویر جدا کند.

ویژگی متمایز:
آموزش روی دیتاست عظیم SA-1B با بیش از یک میلیارد ماسک.

دیتاست: SA-1B

پیاده‌سازی: PyTorch

تعداد ارجاعات: 9,200+

۵. DINOv2

Learning Robust Visual Features without Supervision

نویسندگان: Maxime Oquab و همکاران

سال / محل انتشار: 2023 – arXiv

ایده اصلی:
یادگیری خودنظارتی روی مجموعه‌داده‌ای بسیار بزرگ و پاک‌سازی‌شده برای استخراج ویژگی‌های بصری عمومی و مقاوم.

نکته مهم:
ویژگی‌های تولیدشده حتی بدون فاین‌تیون نیز روی بسیاری از وظایف عملکرد بالایی دارند.

دیتاست: حدود 142 میلیون تصویر بدون برچسب

پیاده‌سازی: PyTorch

تعداد ارجاعات: 4,700+

📊 جدول خلاصه مقالات
#	نوع مقاله	سال	محل انتشار	محور اصلی	دیتاست	کد	Citation
1	پژوهشی	2020	ICLR	معرفی ViT	JFT-300M, ImageNet	vision_transformer	75314
2	پژوهشی	2021	ICCV	Swin Transformer	ImageNet, COCO	Swin-Transformer	24313
3	پژوهشی	2022	CVPR	MAE	ImageNet-1K	mae	8846
4	پژوهشی	2023	ICCV	SAM	SA-1B	segment-anything	9223
5	پژوهشی	2023	arXiv	DINOv2	~142M images	dinov2	4734
🧠 چرا این مقالات به هم مرتبط‌اند؟

همگی در حوزه یادگیری عمیق و هوش مصنوعی قرار دارند

محور مشترک آن‌ها Transformerها و مدل‌های پایه در بینایی ماشین است

یک مسیر تکاملی را نشان می‌دهند:
ViT → Swin → MAE → SAM → DINOv2

🔑 پنج کلیدواژه مهم

Transformer Architecture
ساختار مبتنی بر attention که هسته اصلی همه این مدل‌هاست.

Self-Supervised Learning
یادگیری بدون نیاز به برچسب، پایه MAE و DINOv2 و بخش مهمی از SAM.

Representation Learning
استخراج ویژگی‌های عمومی و انتقال‌پذیر از داده‌های خام.

Foundation Models
مدل‌های بزرگ و چندمنظوره که روی وظایف مختلف قابل استفاده‌اند.

Vision Transformer (ViT)
نقطه شروع تحول ترنسفورمرها در بینایی ماشین.

📚 جمع‌بندی

این پنج مقاله، سیر تحول بینایی ماشین مدرن را از مدل‌های تخصصی عمیق به سمت مدل‌های پایه، عمومی و چندمنظوره نشان می‌دهند؛ مسیری که از ViT آغاز شده و به مدل‌هایی مانند SAM و DINOv2 رسیده است
